<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Date</title>
  </head>
  <body>
    <h1>Today</h1>
    <p>

      <% t = Time.now %>

      <%= "The date is #{t.strftime("%A")}, #{t.strftime("%B")} #{t.strftime("%d")}, #{t.strftime("%Y")}" %>
    </p>

  </body>
</html>
